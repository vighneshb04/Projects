import type { ThirdwebClient } from "../../../../client/client.js";
import type { Ecosystem } from "../wallet/types.js";
import type { ClientScopedStorage } from "./client-scoped-storage.js";
import type { Profile } from "./types.js";
/**
 * @description
 * Links a new account to the current one using an auth token.
 * For the public-facing API, use `wallet.linkProfile` instead.
 *
 * @internal
 */
export declare function linkAccount({ client, ecosystem, tokenToLink, storage, }: {
    client: ThirdwebClient;
    ecosystem?: Ecosystem;
    tokenToLink: string;
    storage: ClientScopedStorage;
}): Promise<Profile[]>;
/**
 * @description
 * Gets the linked accounts for the current user.
 * For the public-facing API, use `wallet.getProfiles` instead.
 *
 * @internal
 */
export declare function getLinkedProfilesInternal({ client, ecosystem, storage, }: {
    client: ThirdwebClient;
    ecosystem?: Ecosystem;
    storage: ClientScopedStorage;
}): Promise<Profile[]>;
//# sourceMappingURL=linkAccount.d.ts.map